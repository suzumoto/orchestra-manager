# 本番運用手順（ザムスターク管弦楽団）

`feature/zamstark-format` ブランチをザムスターク管弦楽団の Discord サーバーで
運用するための手順。汎用的なセットアップは README.md を参照。

## 1. clone とブランチ

```
git clone https://github.com/suzumoto/orchestra-manager.git
cd orchestra-manager
git checkout feature/zamstark-format
```

## 2. Python 環境

- Python 3.11 以上（動作確認は 3.14）
- 依存のインストール:
  ```
  pip install -r requirements.txt
  ```

## 3. 秘密ファイルの配置（git に含まれていないもの）

リポジトリ直下に以下の 2 つを置く。**どちらも git にコミットしないこと**
（.gitignore 済み）。

1. **credentials.json** — Google サービスアカウントの鍵 (JSON)
   - スプレッドシート「第11回定期演奏会 出欠シート(2026)」に**編集者**、
     座席レイアウトのスライドに**閲覧者**として、このサービスアカウントの
     メールアドレスが共有されていること（既存の鍵を使い回すなら共有設定済み）
2. **.env** — `.env.example` をコピーして 3 つの値を記入
   ```
   DISCORD_BOT_TOKEN=<BotのToken>
   SPREADSHEET_ID=<出欠シートのID>
   SLIDES_PRESENTATION_ID=<座席レイアウトスライドのID>
   ```

## 4. settings.ini（コミット済み・変更不要）

本番用の値でコミットされている。前提として Discord サーバー側に以下が
存在すること（名前は完全一致）:

- **チャンネル**: `📅カレンダー`（RSVP投稿・リマインドスレッド）/
  `出欠管理システム`（$コマンド受付。他チャンネルではコマンド無効）/
  `🎎出欠表出力`（出欠表画像の自動出力先）
- **ロール**: `運営`・`サーバー管理`（コマンド実行権限）/
  `第11回演奏会参加者(2026)`（シートに登録するメンバーの対象）/
  各パートロール（`Fl.` `Ob.` など。`[PART_ROLE]` の正規表現と一致する名前）
- **カスタム絵文字**: `shusseki` `kesseki` `chikoku` `soutai` `shutsuryoku`

## 5. Discord Developer Portal の設定

- Privileged Gateway Intents: **SERVER MEMBERS INTENT** と
  **MESSAGE CONTENT INTENT** を ON
- Bot の権限: メッセージ送信 / メッセージ履歴の閲覧 / リアクションの追加・削除 /
  公開・プライベートスレッドの作成 / スレッド内送信 / ファイル添付
- プライベートスレッド作成にはサーバーブースト Lv.2 が必要。
  無い場合は自動で**公開スレッド**にフォールバックする（仕様）

## 6. 起動

```
python orch_bot.py
```

起動直後の挙動（チャンネルへの投稿は行われない）:

1. 参加者ロールのメンバーで**シート未登録の人だけ**を全奏シートへ追加
   （分奏の A〜I 列は全奏参照のため自動反映）。追加があればパート順に
   並べ替え、分奏のイベント列も追従
2. 📅カレンダーの直近 20 件から最新のリアクション状況をシートへ再同期

以後の自動動作:

- 📅カレンダーへの新規投稿（日付行に全奏/分奏を含むもの）に出欠リアクションを
  自動付与し、シートに列を作成。**トップ練・連絡事項には何もしない**
- 毎日 19:00 JST に 7日前 / 3日前 / 前日 のリマインド
  （未回答者がいる場合のみ。スレッド「出欠リマインド M月D日」に投稿）
- 練習日当日 7:00 JST に出欠表画像を 🎎出欠表出力 へ自動投稿
- 遅刻/早退リアクションにはスレッドで時刻入力ボタンを案内

## 7. 常時起動の注意

- 19:00 / 7:00 の時点でプロセスが落ちていると、その回のリマインド・出力は
  **飛ばない**（追い掛け送信なし）。常時起動を維持すること
  （Windows: タスクスケジューラや NSSM、Linux: systemd 等で自動再起動を推奨）
- 実行時に自動生成されるファイル（消えても再生成される）:
  `reaction_order.json`（リアクションを付けた順の記録。消すと再起動後の
  順序解決が固定優先順位になるだけで動作は継続）/
  `.seat_layout_cache/`（スライドのキャッシュ）/ `generated/`（出力画像）

## 8. 運用ルール

| やりたいこと | 操作 |
|---|---|
| 練習の中止 | シートで該当練習日の**列を非表示**にする（リマインド・当日出力が止まる。再表示で再開） |
| メンバー情報の修正 | **全奏シートだけ**を手で直す（分奏 A〜I は数式参照なので直接入力禁止。壊れたら分奏 A3 に `=ARRAYFORMULA('全奏'!A3:I)` を置き直す） |
| メンバーの追加 | 参加者ロールを付けて `$syncmembers`（または起動時同期に任せる） |
| 行の並べ替え | `$sortmembers`（全奏をソート→分奏イベント列を自動追従） |
| 出欠の強制再同期 | `$sync` |
| リマインドの手動実行 | `$remind`（全オフセット）/ `$remind 3`（3日前分のみ） |
| 当日出欠表の手動出力 | `$outputtoday` |
| 乗り番の個別登録 | `$append <プログラム> <パート> <席次> @メンバー` |

$コマンドはすべて `出欠管理システム` チャンネル・運営/サーバー管理ロール限定。
